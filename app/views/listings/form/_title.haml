-if @listing_presenter.category_id == 5
  = form.label :title, t(".listing_title") + "*", :class => "input"
  = form.text_field(:title, :class => "title_text_field", :maxlength => "60", :value => "I want to Invest - @current_user" , :readonly=>true)

-if @listing_presenter.category_id == 3
  = form.label :title, t(".listing_title") + "*", :class => "input"
  = form.text_field(:title, :class => "title_text_field", :maxlength => "60", :value => "I want to Borrow - @current_user"  , :readonly=>true)
