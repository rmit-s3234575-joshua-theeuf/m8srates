- value = custom_field_value ? custom_field_value.ranger : ""
= range_field:range,5,:min=>0,:max=>100, "custom_fields[#{custom_field.id}]", value, :class => (custom_field.required? ? "required" : "")
